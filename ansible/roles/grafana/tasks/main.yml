- name: install required packages for grafana repo
  apt:
    name:
      - apt-transport-https
      - software-properties-common
      - wget
      - gnupg
    state: present
    update_cache: yes

- name: ensure grafana keyrings directory exists
  file:
    path: /etc/apt/keyrings
    state: directory
    mode: "0755"

- name: download grafana gpg key
  get_url:
    url: https://apt.grafana.com/gpg.key
    dest: /tmp/grafana.gpg.key
    mode: "0644"

- name: install grafana gpg key
  shell: |
    gpg --dearmor < /tmp/grafana.gpg.key > /etc/apt/keyrings/grafana.gpg
  args:
    creates: /etc/apt/keyrings/grafana.gpg

- name: configure grafana apt repository
  copy:
    dest: /etc/apt/sources.list.d/grafana.list
    content: |
      deb [signed-by=/etc/apt/keyrings/grafana.gpg] https://apt.grafana.com stable main
    owner: root
    group: root
    mode: "0644"

- name: install grafana
  apt:
    name: "{{ grafana_package_name }}"
    state: present
    update_cache: yes

- name: ensure grafana datasource directory exists
  file:
    path: "{{ grafana_datasource_dir }}"
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: deploy prometheus datasource provisioning
  template:
    src: prometheus-datasource.yml.j2
    dest: "{{ grafana_datasource_dir }}/prometheus.yml"
    owner: root
    group: root
    mode: "0644"
  notify: restart grafana

- name: ensure grafana dashboard provisioning directory exists
  file:
    path: "{{ grafana_dashboards_dir }}"
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: ensure grafana dashboard files directory exists
  file:
    path: "{{ grafana_dashboards_files_dir }}"
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: deploy grafana dashboard provider provisioning
  template:
    src: dashboards-provider.yml.j2
    dest: "{{ grafana_dashboards_dir }}/uptime-monitor.yml"
    owner: root
    group: root
    mode: "0644"
  notify: restart grafana

- name: deploy kubernetes overview dashboard
  copy:
    src: kubernetes-overview-dashboard.json
    dest: "{{ grafana_dashboards_files_dir }}/kubernetes-overview-dashboard.json"
    owner: root
    group: root
    mode: "0644"
  notify: restart grafana

- name: deploy uptime monitor app dashboard
  copy:
    src: uptime-monitor-app-dashboard.json
    dest: "{{ grafana_dashboards_files_dir }}/uptime-monitor-app-dashboard.json"
    owner: root
    group: root
    mode: "0644"
  notify: restart grafana

- name: ensure grafana is enabled and started
  systemd:
    name: "{{ grafana_service_name }}"
    enabled: yes
    state: started
