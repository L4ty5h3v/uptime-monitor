apiVersion: v1
kind: Service
metadata:
  name: uptime-monitor-api
  namespace: dev
  labels:
    app.kubernetes.io/name: uptime-monitor
    app.kubernetes.io/component: api
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: uptime-monitor
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
